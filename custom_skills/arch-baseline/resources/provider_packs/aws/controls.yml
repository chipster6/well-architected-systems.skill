controls:
  - control_id: CTRL-IDENTITY-001
    category: identity_access
    provider_reference: iam
    required_for:
      - all_workloads
    evidence_required:
      - iam_policy_snapshot
    normalization_target:
      registry: registries/security_controls_catalog.yml
      field: controls[].provider_reference
  - control_id: CTRL-LOGGING-001
    category: logging_audit
    provider_reference: cloudtrail_cloudwatch_logs
    required_for:
      - all_workloads
    evidence_required:
      - audit_log_configuration
    normalization_target:
      registry: registries/security_controls_catalog.yml
      field: controls[].evidence_required

